import { useState, useMemo, useEffect, useRef } from 'react';
import { Search, Radar, Flame, Image as ImageIcon, Loader2 } from 'lucide-react';
import { GridCard } from './PostCard';
        </div>
      )}
    </div>
  );
}
